# Lab 04 EDA (Titanic notebook)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Optional later (not required for the starter notebook)
# scikit-learn>=1.3
# missingno>=0.5

# Local notebook tooling
pytest>=7.4
jupyter>=1.0
nbconvert>=7.0

# PDF export tooling (same approach as Module 3: nbconvert --to html
# with a wrap-CSS override, rendered to PDF via local Playwright/Chromium)
playwright>=1.40
